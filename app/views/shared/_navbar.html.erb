<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Scheduled Tweets", root_path, class: "navbar-brand" %>
    <div class="d-flex">
      <div class="expand navbar-expand" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link" %>
          </li>

          <% if Current.user %>
          <li class="nav-item">
            <%= link_to "Account", account_path, class: "nav-link" %>
          </li>
          <% else %>
          <li class="nav-item">
            <%= link_to "About", about_path, class: "nav-link" %>
          </li>
          <% end %>

          <% unless Current.user %>
          <li class="nav-item">
            <%= link_to "Sign In", sign_in_path, class: "nav-link" %>
          </li>
          <% end %>

        </ul>
      </div>
      <% if Current.user %>
        <%= button_to "Log out", logout_path, method: :delete, class: "btn btn-light" %>
      <% end %>
    </div>
  </div>
</nav>
